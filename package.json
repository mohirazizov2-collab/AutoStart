{
  "name": "avto24",
  "version": "1.0.0",
  "description": "AVTO24 avtomobil bozori",
  "main": "api/index.js",
  "scripts": {
    "start": "node backend/server.js",
    "dev": "cd backend && npm run dev",
    "install-all": "npm install && cd backend && npm install"
  },
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  }
}
